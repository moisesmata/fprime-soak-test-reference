command-line-options:
  no-app:
  dictionary: ../../build-artifacts/Darwin/FprimeSoakTestReference_FprimeSoakTestReferenceDeployment/dict/FprimeSoakTestReferenceDeploymentTopologyDictionary.json
  communication-selection: udp
  # Must match FSW -p (GDS listen / FSW send destination).
  udp-port: 50000
  # Same Space Packet framing as the radio deployment (no TM/TC transfer frames).
  framing-selection: raw-space-packet
  gui-port: 5011
  no-zmq:
